## Clamp x to [lo, hi], then add shift to the clamped value.
## Precondition: hi >= lo (encoded in the type of hi).
## Contract: result >= lo + shift AND result <= hi + shift (proved at compile time).
fn clamp_then_shift(x: Int, lo: Int, hi: Int{h: h >= lo}, shift: Int) -> Int
  where result >= lo + shift && result <= hi + shift
  effects {} {
  0
}
