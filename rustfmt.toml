max_width = 100
use_field_init_shorthand = true
# imports_granularity and group_imports require nightly rustfmt.
# Uncomment when the workspace upgrades to a nightly formatter channel.
# imports_granularity = "Module"
# group_imports = "StdExternalCrate"
