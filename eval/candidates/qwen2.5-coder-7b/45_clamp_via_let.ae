fn clamp_via_let(x: Int, lo: Int, hi: Int{h: h >= lo}) -> Int
  where result >= lo && result <= hi
  effects {} {
    if x < lo {
      let bounded = lo;
      bounded
    } else if x > hi {
      let bounded = hi;
      bounded
    } else {
      let bounded = x;
      bounded
    }
}
