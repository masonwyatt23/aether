target/
playground/pkg/
playground/node_modules/
vscode-aether/node_modules/
vscode-aether/out/
**/*.rs.bk
Cargo.lock.bak
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~

# Python (eval harness + RL environment)
__pycache__/
*.pyc

# Generated benchmark task sets — regenerable via `gen_tasks.py` from a seed,
# and deliberately not committed (a fixed instance set would be a
# contamination vector).
eval/instances/
