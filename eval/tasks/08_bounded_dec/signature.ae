## Decrement x by 1, with floor lo <= x supplied as a precondition.
## Contract: result <= x AND result >= lo - 1 (proved at compile time).
fn bounded_dec(x: Int, lo: Int{l: l <= x}) -> Int
  where result <= x && result >= lo - 1
  effects {} {
  0
}
