## Return the absolute difference |a - b|.
## Contract: result >= 0, result >= a - b, AND result >= b - a (proved at compile time).
fn abs_diff(a: Int, b: Int) -> Int
  where result >= 0 && result >= a - b && result >= b - a
  effects {} {
  0
}
