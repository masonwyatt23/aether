## Decrement x, but floor the result at 0.
## Precondition: x >= 0 (encoded in x's type).
## Contract: result >= 0 (proved at compile time).
fn safe_pred(x: Int{v: v >= 0}) -> Int
  where result >= 0
  effects {} {
  x - 1
}
