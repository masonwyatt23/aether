## Clamp x to the discrete unit interval {0, 1}.
## Contract: result >= 0 AND result <= 1 (proved at compile time).
fn clamp_unit(x: Int) -> Int
  where result >= 0 && result <= 1
  effects {} {
  0
}
