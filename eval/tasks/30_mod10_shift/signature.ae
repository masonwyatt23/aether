## Return (n % 10) + 1, proved in range [1, 10].
## Contract: result >= 1 AND result < 11 (proved at compile time).
fn mod10_shift(n: Int) -> Int
  where result >= 1 && result < 11
  effects {} {
  0
}
