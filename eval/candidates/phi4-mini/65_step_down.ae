fn step_down(x: Int{v: v >= lo && v <= hi}, lo: Int, hi: Int{h: h >= lo}, step: Int{s: s >= 0}) -> Int
  where result >= lo && result <= x
  effects {} {
    return (x - step) < lo ? lo : x - step;
  }
}
