## Decrement x by 1 but floor the result at lo.
## Precondition: lo <= x (encoded in lo's type).
## Contract: result >= lo (proved at compile time).
fn prev_capped(x: Int, lo: Int{l: l <= x}) -> Int
  where result >= lo
  effects {} {
  if x >= lo + 1 then x - 1 else lo
}
