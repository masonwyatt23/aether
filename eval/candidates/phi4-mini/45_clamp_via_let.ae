fn clamp_via_let(x: Int, lo: Int, hi: Int{h: h >= lo}) -> Int
  where result >= lo && result <= hi
  effects {} {
    let bounded = if x < lo then lo else if x > hi then hi else x;
    return bounded;
}
