## Return -1, 0, or 1 depending on the sign of n.
## Contract: result >= -1 AND result <= 1 (proved at compile time).
fn sign(n: Int) -> Int
  where result >= -1 && result <= 1
  effects {} {
  if n > 0 then 1 else if n < 0 then 0 - 1 else 0
}
